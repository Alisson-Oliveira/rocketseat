import Usuario, { idade } from './functions';
import Usuario, { idade as IdadeUsuario } from './functions';

Usuario.info();
console.log(idade);
console.log(IdadeUsuario);

// É necessario baixar e instalar o webpack na pasta do projeto. nesta pasta não está instalado.
// Qualquer duvida, só olhar o arquivo "main.js" da pasta "src" do projeto Curso-ES6.

